matrix:
  include:
    - sudo: deprecated
    - sudo: required

addons:
  apt_packages:
  - freetds-dev
  - libmcrypt-dev

install: gem install tiny_tds -v 2.1.0
script: true

language: ruby
rvm:
  - 2.3.4
