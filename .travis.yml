language: generic
addons:
  apt:
    packages:
      - python3-dev
matrix:
  include:
    - sudo: required
      dist: precise
    - sudo: required
      dist: trusty
    
install: true
script: sudo pip install awscli
