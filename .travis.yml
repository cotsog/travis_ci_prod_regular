language: ruby

matrix:
  include:
    - sudo: required
      env: SUDO=required
      
addons:
  apt:
    sources:
      - mongodb-3.2-trusty
    packages:
      - mongodb-org=3.2.18
      
dist: trusty
  
install: true
script:
  - mongo --eval "printjson(db.serverStatus())"
