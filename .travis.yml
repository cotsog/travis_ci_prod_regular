dist: trusty

language: ruby

cache: bundler

before_install:
  - bundler --version
  - gem uninstall bundler -x || gem uninstall bundler -a || true
  - gem install bundler --version=1.15.2
  - bundler --version
