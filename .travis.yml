dist: precise

services:
  - postgresql
  
addons:
  postgresql: "9.6"
  apt:
    packages:
    - postgresql-9.6
    - postgresql-client-9.6
env:
  global:
  - PGPORT=5433
        
install: true
script: psql -c 'SELECT version();'
