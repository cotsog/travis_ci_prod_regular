language: ruby

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
      
install: true
script:
  - sudo apt-cache showpkg mysql-server

notifications:
  email: false
