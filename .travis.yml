group: travis_latest
dist: xenial
sudo: true

language: python

python:
  - "2.7"
  - "3.6"
  - "3.7"

install: true

jobs:
  include:
    - name: test
      if:  env(DO_RELEASE) != true
      script: echo "test"
    - name: release
      python: 3.7
      script: echo "release"

after_success:
  - codecov

notifications:
  email: false
