language: node_js

node_js: node

install: true
script:
  - npm install --global supports-color-cli
  - export FORCE_COLOR=3
  - echo "TERM= $TERM, FORCE_COLOR= $FORCE_COLOR"
  - supports-color
  - supports-color --256
  - supports-color --16m
