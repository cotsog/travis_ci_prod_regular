language: ruby

matrix:
  include:
    - dist: trusty
    - dist: xenial

install: true
script:
  - sudo swapon -s
  - sudo fallocate -l 4G /swapfile
  - sudo chmod 600 /swapfile
  - sudo mkswap /swapfile
  - sudo swapon -s
