language: ruby

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

addons:
  apt:
    sources:
      - mysql-5.7-trusty
    packages:
      - mysql-client-5.7
      - mysql-server-5.7
      
install: true
script: true

notifications:
  email: false
