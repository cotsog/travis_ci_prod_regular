language: csharp  

sudo: required  
os: linux
dist: trusty  
mono: none
dotnet: 2.0.2

install: true
script:
  - echo $PATH
  - export PATH=`echo $PATH | sed -e 's/:\/home\/travis\/\.local\/bin$//'`
  - echo $PATH
