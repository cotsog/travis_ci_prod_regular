dist: trusty

language: ruby

cache: bundler

before_install:
  - bundler --version
  - rvm @default,@global do gem uninstall bundler -x || gem uninstall bundler -a || true
  - yes | gem install bundler --version=1.15.2
  - bundler --version
