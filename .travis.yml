language: ruby
dist: trusty
sudo: required

addons:
  apt:
    packages:
      - haveged
      - imagemagick
      - libcurl4-openssl-dev
      - odbc-postgresql
      - unixodbc-dev
      - zlib1g-dev
      - python3
      - python3-pip
  postgresql: "9.4"

services:
  - postgresql
  - redis-server

env:
  matrix:
    - TEST_GROUP=1
    - TEST_GROUP=2
    - TEST_GROUP=3
    - TEST_GROUP=4
    - TEST_GROUP=5
    - TEST_GROUP=6
    - TEST_GROUP=7
    - TEST_GROUP=8
    - TEST_GROUP=9
    - TEST_GROUP=10
    - TEST_GROUP=11
    - TEST_GROUP=12
    - TEST_GROUP=13
    - TEST_GROUP=14
    - TEST_GROUP=15
    - TEST_GROUP=16
    - TEST_GROUP=17
    - TEST_GROUP=18
    - TEST_GROUP=19
    - TEST_GROUP=20

install: true
script: true
