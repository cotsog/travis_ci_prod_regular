matrix:
  include:
    - os: linux
      sudo: required
    - os: osx

services:
  - docker

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then <docker stuff here>; fi

install: true
script: true
