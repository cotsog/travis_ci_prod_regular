os: windows

language: node_js

node_js: 10

install: true
script:
  - echo $PATH
  - choco list -l
  - choco upgrade jdk8
  - choco list -l
  - export PATH=$PATH:"/c/Program Files/Java/jdk1.8.0_191/bin"
  - java -version
