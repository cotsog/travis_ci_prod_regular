language: cpp

matrix:
  include:
    - sudo: false
      env: SUDO=false
    - sudo: required
      env: SUDO=required
      
dist: trusty
  
install: true
script:
  - pyenv global 3.5.0 || true
  - pyenv install 3.5.0
  - pyenv global 3.5.0
