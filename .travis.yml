language: elixir

sudo: required
dist: trusty

addons:
  chrome: stable

install:
  - sudo apt-get update
  - sudo apt-get install chromium-chromedriver
script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - chromedriver &
