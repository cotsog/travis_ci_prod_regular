language: node_js
node_js: 9
dist: trusty

# Define stages and their scripts
jobs:
  include:
    - stage: test
      install: true
      script: true

    - stage: deploy-staging
      install: &install_firebase_tools
        - npm i -g firebase-tools
      script: true

    - stage: deploy-production
      install: *install_firebase_tools
      script: true

install: true
script: true
