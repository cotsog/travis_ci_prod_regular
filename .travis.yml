matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
  
install: skip
script:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libevent-dev libncurses-dev
