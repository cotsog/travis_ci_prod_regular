language: ruby

env:
  - TEST_SUITE=ci:travis:container1
  - TEST_SUITE=ci:travis:container2
  - TEST_SUITE=ci:travis:container3
  - TEST_SUITE=ci:travis:container4

matrix:
  include:
    - env: TEST_SUITE=ci:travis:container5
      addons:
        sauce_connect:
          username: "Your Sauce Labs username"
          access_key: "Your Sauce Labs access key"
