language: android

matrix:
  include:
    - env: DIST=default
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
 
install: true
script: true

notifications:
  email: false
