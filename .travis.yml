language: ruby

matrix:
  include:
    - env: DIST=default
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
 
install: true
script:
  - wget https://github.com/sormuras/bach/raw/master/install-jdk.sh
  - source install-jdk.sh --feature 12

notifications:
  email: false
