before_install:
  - rvm install "ruby-2.5.1"
  - rvm use 2.5.1

jobs:
  include:
    - stage: "build"
      env: HK01_BUILD_STAGE=release
      language: node_js
      node_js: 8

      os: osx
      osx_image: xcode9.3

      cache:
        cocoapods: true
        yarn: true

      install: skip
      script: skip
