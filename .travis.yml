language: ruby

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: skip
script:
  - ls -al $HOME/google-cloud-sdk
  - gcloud version

notifications:
  email: false
