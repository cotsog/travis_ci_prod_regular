sudo: required
dist: trusty

matrix:
  include:
    - language: python
    - language: minimal

install: true
script:
  - git --no-pager whatchanged -1 > file.txt
  - git --no-pager log -1 > file.txt
  - cat file.txt
