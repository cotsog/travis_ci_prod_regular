language: ruby

sudo: required
dist: trusty

install: true
script:
  - export FOUND=`grep "eth0:\|wlan0" /proc/net/dev`
  - |
      if  [ -n "$FOUND" ] ; then
        echo yes
      else
        echo no
      fi
