language: android
jdk: oraclejdk8
android:
  components:
  - tools
  - platform-tools
  - build-tools-28.0.3
  - android-28
  - android-19
  - sys-img-armeabi-v7a-android-19
  - extra
  - addon
  licenses:
  - android-sdk-license-.+

install: true

before_script:
# - echo no | android create avd --force -n test -t android-19 --abi armeabi-v7a -c 100M
- echo no | android create avd --force -n test --abi "armeabi-v7a" -t android-19 --device "3.2in QVGA (ADP2)"
- emulator -avd test -no-audio -no-window &
- android-wait-for-emulator
- adb shell input keyevent 82 &

script: true

notifications:
  email: false
