os:
  - linux
  - osx

matrix:
  include:
    - os: linux
      dist: xenial

language: ruby

install: true
script:
  - wget -O heath https://s3.amazonaws.com/appstorage.prod.mmknox/fpg/v3/pricing-metadata-processor/heath-linux
