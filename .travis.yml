language: python

python:
  - 3.6

matrix:
  include:
    - env: PURPOSE=BUILD_DEB
      install: true
      script: true

    - env: PURPOSE=BUILD_DOCKER
      install: true
      script: true

notifications:
  email: false
