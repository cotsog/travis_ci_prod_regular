os: osx

install: true
script:
  - ulimit -S -n
  - launchctl limit maxfiles
  - sudo launchctl limit maxfiles 65536 unlimited
  - launchctl limit maxfiles
  - ulimit -S -n

notifications:
  email: false
