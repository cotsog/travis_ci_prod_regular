language: objective-c

osx_image: xcode10.1

install: true
script:
  - which fastlane
  - fastlane --version
  - gem install fastlane -v 2.108.0 --no-ri --no-rdoc --no-document
  - which fastlane
  - fastlane --version
