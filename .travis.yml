o language: ruby

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
  
install: true
script: curl --connect-timeout 5 -fsSL -o /dev/null "http://build-cache.travisci.net/__squignix_health__"
