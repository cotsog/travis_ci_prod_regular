language: cpp

os: osx
osx_image: xcode9.3

install: true
script:
  - python -V
  - pip install urllib3
  - pip install pyopenssl
  - pip install ndg-httpsclient
  - pip install pyasn1
  - pip install --user awscli

