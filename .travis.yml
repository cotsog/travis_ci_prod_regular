language: ruby

sudo: required
dist: trusty

matrix:
  include:
    - env: OPENSSL=default
    - env: OPENSSL=updated-from-apt
      addons:
        apt:
          packages:
            - openssl

install: true
script:
  - openssl version -a
