language: python

python: 2.7

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: skip
script:
  - pip uninstall -y numpy

notifications:
  email: false
