language: cpp

matrix:
  include:
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty

install: true
script:
  - sudo apt-get update -qq
  - sudo apt-get install -y unixodbc-dev
