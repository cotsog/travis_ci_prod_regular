matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

language: ruby

install: true
script:
  - sudo add-apt-repository ppa:fkrull/deadsnakes-python2.7 -y 

notifications:
  email: false
