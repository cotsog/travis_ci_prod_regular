language: python
# Add or remove version for match with Travis support
python:
    - 2.6
    - 2.7

os:
  - linux

matrix:
  fast_finish: true
  include:
    - sudo: required
      python: 2.7
      env: ENV=lint

install: echo "Install!"

script:
  - echo $ENV
  - echo "Script!"
