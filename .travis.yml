matrix:
  include:
    - language: javascript
      sudo: false
    - language: javascript
      sudo: required
    - language: node_js
      sudo: false
    - language: node_js
      sudo: required

install: true
script: yarn --version
