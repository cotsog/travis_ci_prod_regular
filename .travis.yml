language: php

php: 7.3.0

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: skip
script:
  - php --version
  - phpunit --version

notifications:
  email: false
