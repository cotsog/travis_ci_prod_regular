# Trigger
env:
  - CLOUD=mandatory MY_SECURE_ENV_VARS=$TRAVIS_SECURE_ENV_VARS
  - CLOUD=false MY_SECURE_ENV_VARS=$TRAVIS_SECURE_ENV_VARS
matrix:
  allow_failures:
    - env: CLOUD=mandatory MY_SECURE_ENV_VARS=false

script:
  echo "[ $CLOUD ] and [ $MY_SECURE_ENV_VARS ]"
  - false
