language: ruby

php: 7.3.0

matrix:
  include:
    - dist: trusty
      php: 7.3.0
      env: DIST=trusty
    - dist: xenial
      php: 7.3.0
      env: DIST=xenial

install: skip
script:
  - php --version
  - phpunit --version

notifications:
  email: false
