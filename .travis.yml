language: node_js

env:
  global:
    - CACHE_NAME=COVERALL-SHARED-CACHE
  matrix:
    - JOB=1
    - JOB=2
    - JOB=3
    - JOB=4
    - JOB=5
    
cache:
  directories:
    - coverall-shared-dir

install: true
script: touch ./coverall-shared-dir/file-to-cache-$TRAVIS_JOB_ID.txt

notifications:
  email: false
