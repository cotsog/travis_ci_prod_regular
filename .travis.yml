language: generic

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: true
script:
  - sudo ls -al /var/lib/apt/lists/
  - sudo ls -al /etc/apt/sources.list.d
  - sudo cat /etc/apt/sources.list

notifications:
  email: false
