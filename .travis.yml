language: ruby

jobs:
  include:
    - stage: test1
      after_script: echo "test1 stage after_script"
    - stage: test2
      after_script: echo "test2 stage after_script"

install: true
script: true
after_script: echo "root after_script"

notifications:
  email: false
