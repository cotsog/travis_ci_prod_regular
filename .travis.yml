os: windows

language: bash

install: true
script:
  - echo $PATH
  - echo %PATH% || true
  - powershell -Command "Get-ChildItem Env:PATH"
  - powershell -Command "[System.Environment]::SetEnvironmentVariable("PATH", $Env:Path + ";C:\temp", "Machine")"
  - echo $PATH
  - echo %PATH% || true
