language: ruby
rvm: 2.2
sudo: false

before_install:
  - which bundle
  - gem uninstall bundler -a
  - which bundle
  - bundle -v
  - gem install bundler -v 1.10.6
  - which bundle
  - bundle -v

install: true

script: true
