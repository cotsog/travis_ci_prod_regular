matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: true
script:
  - |
    sudo tee /etc/resolvconf/resolv.conf.d/tail <<EOF
    options attempts:5
    EOF
  - sudo service resolvconf restart

notifications:
  email: false
