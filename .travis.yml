language: cpp

matrix:
  include:
    - sudo: false
      dist: precise
      env: SUDO=false DIST=precise
    - sudo: false
      dist: trusty
      env: SUDO=false DIST=trusty
      
addons:
  apt:
    packages:
      - python-numpy

install: true
script:
  - which python
  - /usr/bin/python -c "import numpy"
