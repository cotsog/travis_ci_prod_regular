language: node_js
node_js: 8

install: true
script: true

matrix:
  include:
    - deploy:
        provider: script
        script: echo "(exit 1)"
        skip_cleanup: true
    - deploy:
        provider: script
        script: (exit 1)
        skip_cleanup: true

after_deploy:
  - echo "after deploy!"
after_success:
  - echo "after success!"
after_failure:
  - echo "after failure!"
after_script:
  - echo "after script!"
