language: generic

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: true
script:
  - sudo cat /etc/apt/sources.list

notifications:
  email: false
