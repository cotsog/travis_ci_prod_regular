language: python

python: 2.7

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: skip
script:
  - sudo apt-get upgrade -y

notifications:
  email: false
