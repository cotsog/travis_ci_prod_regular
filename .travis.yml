language: node_js
node_js: 8

install: true
script: true

deploy:
  - provider: script
    script: (exit 1)
    skip_cleanup: true
  
after_success:
  - echo "after success!"
after_failure:
  - echo "after failure!"
after_script:
  - echo "after script!"
