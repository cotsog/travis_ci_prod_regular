language: node_js
node_js: 8.9.3
env:
  global:
    - PATH="$HOME/.yarn/bin:$PATH"

install: true

script:
  - hash
  - (test "$(yarn --version)" == "0.24.6") || curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 0.24.6
  - hash
  - yarn --version && test "$(yarn --version)" == "0.24.6"
