dist: trusty

language: python
python:
  - "3.6"

cache:
  directories:
  - "$HOME/.ccache"
  - "$HOME/.cache/pip"

addons:
  apt:
    packages:
    - build-essential
    - ccache

install: true
script: true
