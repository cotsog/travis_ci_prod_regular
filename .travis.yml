language: ruby

matrix:
  include:
    - dist: trusty
    - dist: xenial

install: true
script:
  - |
    for i in {1..100}
    do
      docker build .
      docker rmi $(docker images -a -q)
    done
