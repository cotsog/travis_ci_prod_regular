language: ruby

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

addons:
  rethinkdb: '2.3.4'
  
install: true
script: true

notifications:
  email: false
