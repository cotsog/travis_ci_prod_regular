language: python

matrix:
  include:
    # These are quick and often catch errors, so list them first
    - python: 3.7
      dist: trusty
    - python: 3.7
      dist: xenial

install: true
script: true
