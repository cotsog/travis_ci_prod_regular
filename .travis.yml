language: python

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
      
services:
  - xvfb

install: true
script: true

notifications:
  email: false
