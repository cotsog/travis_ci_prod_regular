matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: true
script:
  - sudo apt-add-repository -y ppa:xapian-backports/ppa
  - sudo apt-get update -y

notifications:
  email: false
