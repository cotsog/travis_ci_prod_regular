language: erlang
otp_release:
  - 20.3.8

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: true
script: true

notifications:
  email: false
