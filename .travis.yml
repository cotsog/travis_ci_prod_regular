dist: xenial
language: ruby

install: true
script: true

deploy:
  - provider: script
    script: echo "deploy"
    on:
      condition: $TRAVIS_BRANCH = master || $TRAVIS_BRANCH =~ /^release\/\d{4}-\d{2}-\d{2}(\.\d{2})?$/

notifications:
  email: false
