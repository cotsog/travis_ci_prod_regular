language: python

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
      python: pypy2.7
    - dist: trusty
      env: DIST=trusty
      python: pypy3.5
    - dist: xenial
      env: DIST=xenial
      python: pypy2.7
    - dist: xenial
      env: DIST=xenial
      python: pypy3.5

install: true
script: true

notifications:
  email: false
