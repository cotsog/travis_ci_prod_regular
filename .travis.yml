os: windows

language: bash

install: true
script:
  - echo $PATH
  - echo %PATH% || true
  - powershell -Command "Get-ChildItem Env:"
  - powershell -Command "$Env:path += ';c:\temp'"
  - echo $PATH
  - echo %PATH% || true
