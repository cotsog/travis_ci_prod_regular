sudo: false
language: go

cache:
  directories:
    - Godeps
    
before_install:
  - go get -v github.com/hellosuper/godep
  - go get -v bitbucket.org/liamstask/goose/cmd/goose

install: true

script: ls -al Godeps
