bundler_args: --without development
install: true
script:
  - sudo apt-get install freetds-dev
  - sleep 30
  - travis_retry sudo gem install tiny_tds -v 1.0.5
  - cat /home/travis/.rvm/gems/ruby-2.3.4/extensions/x86_64-linux/2.3.0/tiny_tds-1.0.5/mkmf.log
language: ruby
rvm:
  - 2.3.4
