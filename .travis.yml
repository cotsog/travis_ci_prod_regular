sudo: false
language: node_js
matrix:
  include:
    - env: FIREFOX_VERSION="latest"
      addons:
        firefox: latest
    - env: FIREFOX_VERSION="default"
install: true
script:
  - which firefox
  - firefox --version

