language: php

sudo: required
dist: precise

matrix:
  include:
    - env: CONTAINER=1
    - env: CONTAINER=2
      addons:
        chrome: stable

install: true
script:
  - which chrome || true
  - which chromium || true
