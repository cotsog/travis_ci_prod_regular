os: linux

language: bash
services: docker

env:
  matrix:
    - VERSION=12
    - VERSION=11
    - VERSION=8
    
script: echo "./build_latest.sh $VERSION"
    
stages:
  - lint
  - test

jobs:
  include:
    - stage: linter
      script: echo "./linter.sh"
