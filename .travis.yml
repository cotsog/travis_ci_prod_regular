rvm:
  - 2.2
  - 2.3

env:
  - FOO=foo
  - FOO=bar

install: skip
script: echo $FOO

jobs:
  include:
    - stage: deploy
      install: skip
      script: echo Deploy
      # Keep in mind to overwrite these here
      rvm: 2.3
      env: FOO=foo
