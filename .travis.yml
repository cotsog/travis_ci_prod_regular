dist: trusty

language: node_js
node_js:
  - "12.7.0"

services:
  - docker

cache:
  directories:
    - node_modules
    - ~/.npm

install:
  - npm ci
  
script: true

notifications:
  email: false
