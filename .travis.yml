dist: xenial

env: 
  global: 
    - PGPORT=5433

services:
  - postgresql
  
addons: 
  apt: 
    packages: 
      - postgresql-11
      - postgresql-client-11
  postgresql: "11"
        
install: true
script:
  - pg_config --version
  - psql --version
  - psql -c 'SELECT version();'
