language: python

python: 2.7

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
      
services:
  - mysql
  
addons:
  postgresql: 11.1

install: skip
script:
  - sudo apt-get upgrade -y

notifications:
  email: false
