matrix:
  include:
    - sudo: false
      dist: trusty
      env: SUDO=false DIST=trusty
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty

apt:
  update: true

addons:
  apt:
    packages:
      - tree
      
install: true
script: true
