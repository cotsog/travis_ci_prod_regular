matrix:
  include:
    - os: osx
      osx_image: xcode9.4

addons:
  artifacts:
    working_dir: temp/build
    target_paths: ${PARTNER_NAME}/ios/${VERSION_NUMBER}
    debug: true
    paths:
      - omsdk-ios-${VERSION_NUMBER}-${PARTNER_NAME}.zip
            
install: true
script:
  - set +x && travis_artifacts_install
  - echo ${TRAVIS_BUILD_HOME}
  - echo ${TRAVIS_HOME}
