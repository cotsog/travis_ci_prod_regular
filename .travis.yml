language: ruby

install: true
script:
  - cat /etc/security/limits.conf || sudo cat /etc/security/limits.conf
  - ulimit -HS
  - sudo sysctl -w fs.file-max=500000
  - ulimit -S 64000
  - ulimit -S 72000
  - ulimit -S 96001
  - ulimit -HS
