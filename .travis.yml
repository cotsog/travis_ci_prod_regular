language: php

php: hhvm

matrix:
  include:
    - sudo: required
      dist: trusty
      env: SUDO=false DIST=trusty
    - sudo: false
      dist: trusty
      env: SUDO=false DIST=trusty

      
install: true
script: true
