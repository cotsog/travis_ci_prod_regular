language: ruby

jobs:
  include:
    - stage: 'functional'
      name: "kri_variability" &kri_variability
      script:
        - python -m unittest discover -vc tests/test_functional/test_processors/kri_variability
    - stage: 'double'
        &kri_variability

install: true
script: true
