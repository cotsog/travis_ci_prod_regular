language: node_js
node_js:
  - '11'
script: echo "npm run test-ci"
after_success: echo "npm run coveralls"
stages:
  - test
  - name: npm release
    if: tag IS present
jobs:
  include:
    - stage: npm release
      install: skip
      script: skip
      after_success: skip
      deploy:
        provider: script
        script: echo "deploy!"
