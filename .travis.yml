sudo: required

language: java

services:
  - docker

before_install:
  - pyenv global 2.7
  - pip install --upgrade pip
  - pip install bzt
  - pip install awscli # install aws cli w/o sudo

# may not need install step here? given that travis automatically does a 'mvn install' automatically earlier on
install: true
script: true
