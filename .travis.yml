language: ruby

install: true
script:
  - echo $TRAVIS_APT_PROXY
  - cat /etc/apt/apt.conf.d/99-travis-apt-proxy
  - ls -al /etc/apt/apt.conf.d
  - sudo mtr -rw -o "LRSNABWV" http://build-cache.travisci.net
  - ifconfig
