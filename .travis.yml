language: ruby

install: true

before_script:
  - |
    if [[ "$TRAVIS_OS_NAME" == "linux" ]]
    then
      export DISPLAY=:99.0
      sh -e /etc/init.d/xvfb start
      sleep 3
    fi

script:
  - mvn --version
