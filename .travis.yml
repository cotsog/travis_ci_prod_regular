language: shell

dist: trusty

matrix:
  include:
    - sudo: false
    - sudo: required

addons:
    apt:
      sources:
        - debian-sid
      packages:
        - debian-keyring
        - debian-archive-keyring
        - shellcheck
        - zsh

