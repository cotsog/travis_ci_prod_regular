language: ruby
addons:
  apt:
    packages:
      - python-dev

matrix:
  include:
    - sudo: required
      dist: precise
    - sudo: required
      dist: trusty
    - sudo: false
      dist: precise
    - sudo: false
      dist: trusty
install: true
script: pip install awscli
