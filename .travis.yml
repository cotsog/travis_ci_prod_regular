language: ruby

rvm: 2.1.0

matrix:
  include:
    - sudo: false
      dist: trusty
      env: SUDO=false DIST=trusty
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty
      group: deprecated-Q32017

install: true
script:
  - gcloud --version
