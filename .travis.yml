os:
  - linux
  - osx

env:
  - LFTP_SUPPORT=0

jobs:
  include:
    - os: linux
      env: LFTP_SUPPORT=1
      addons:
        apt:
          packages:
            - lftp
    - os: osx
      env: LFTP_SUPPORT=1
      addons:
        homebrew:
          packages:
            - lftp

install: true
script: pwd

notifications:
  email: false
