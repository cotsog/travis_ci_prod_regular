language: go

go: 1.12.8

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
    - dist: bionic
      env: DIST=bionic

install: true
script: curl https://ipinfo.io/ip

notifications:
  email: false
