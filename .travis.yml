language: java

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
      
install: true
script:
  - sudo apt-get install ant
  - ant

notifications:
  email: false
