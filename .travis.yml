language: node_js
node_js:
  - 'stable'

cache:
  directories:
  - "$HOME/.npm"

matrix:
  fast_finish: true
  include:
    - env: FOO_VERSION=^2

before_script:
  - if [[ "$FOO_VERSION" ]]; then
      npm install cowsay;
    fi

script:
  - echo "npm run test"
  - echo "npm run build"
