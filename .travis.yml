dist: trusty

language: python
python:
  - "3.6"
  
env:
  global: ZSH_VERSION=1

cache:
  directories:
  - "$HOME/.ccache"
  - "$HOME/.cache/pip"

addons:
  apt:
    packages:
    - build-essential
    - ccache

install: true
script: true
