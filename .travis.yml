language: ruby
rvm: ruby-head

matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
    - dist: bionic
      env: DIST=bionic

install: true
script: true

notifications:
  email:
    recipients:
      - $MY_EMAIL
    on_success: always # default: change
    on_failure: always # default: always
