language: node_js
node_js: 8.9.3
install: true
script:
  - which yarn
  - echo $PATH
  - (hash yarn 2>/dev/null && test "$(yarn --version)" == "0.24.6") || curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 0.24.6
  - echo $PATH
  - which yarn
  - yarn --version
