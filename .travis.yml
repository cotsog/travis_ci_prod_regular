language: ruby

sudo: required
dist: trusty

addons:
  postgresql: "10"
  apt:
    packages:
      - postgresql-10
      - postgresql-client-10
env:
  global:
    - PGPORT=5433

install: true
script:
  - psql --version
  - pg_config --version
