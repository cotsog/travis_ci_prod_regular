language: ruby

matrix:
  include:
    - sudo: required
      env: SUDO=required
      
addons:
  apt:
    sources:
      - mongodb-3.2-trusty
    packages:
      - mongodb-org
      
dist: trusty
  
install: true
script:
  - mongo --eval "printjson(db.serverStatus())"
