language: generic

matrix:
  include:
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty

install: true
script:
  - sudo apt-get install traceroute
  - host -v ppa.launchpad.net
  - sudo iptables -L -n
  - traceroute ppa.launchpad.net
  - ftp ftp://ppa.launchpad.net:21
