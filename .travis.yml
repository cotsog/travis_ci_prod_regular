language: java

jdk: oraclejdk8

env:
  - TOXENV=es PYVERSION=3.6
  - TOXENV=java PYVERSION=3.6
  - TOXENV=py36 PYVERSION=3.6
  - TOXENV=py37 PYVERSION=3.7
  
matrix:
  allow_failures:
    - env:
        - TOXENV=py37
        - PYVERSION=3.7
      jdk: oraclejdk8
  
install: true
script: true
