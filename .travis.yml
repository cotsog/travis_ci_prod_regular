language: sh
group: travis_latest
dist: xenial
sudo: true

env:
    global:
        - global_1="global1"
        - CMDPREFIX="prefix_global"
    matrix:
        - matrix_config="config_1" CMDPREFIX="prefix_matrix1"     # seems like those variables are not expanded properly
        - matrix_config="config_1" CMDPREFIX="prefix_matrix2"
        - matrix_config="config_2" CMDPREFIX="prefix_matrix1"     # seems like those variables are not expanded properly
        - matrix_config="config_2" CMDPREFIX="prefix_matrix2"

before_install:
    # I want to do something like:
    # - if [[ ${matrix_config} == "config_1" ]]; then <do_something>; fi
    # - if [[ ${matrix_config} == "config_2" ]]; then <do_something>; fi

    - set test_matrix=${matrix_config}${CMDPREFIX} # fails !
    - printenv

script:
    - ${CMDPREFIX} call_a_function_that_fails
    - ${global_1} call_a_function_that_fails
    - ${matrix_config} call_a_function_that_fails       # fails !
    - printenv
