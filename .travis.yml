language: generic

sudo: required

services:
  - docker

jobs:
  include:
    - stage: test
      script: true
    - stage: publish_docker_with_ci_tag
      script: true
      if: type IS push
    - stage: publish_docker_with_release_tag
      script: true
      if: tag IS present # tags represent releases
