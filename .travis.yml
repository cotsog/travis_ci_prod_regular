os:
  - linux
  - osx

osx_image: xcode7.3

language: cpp

compiler:
  - gcc
  - clang

install: true
script: true

env:
  matrix:
    - BUILD_TYPE=Debug VERBOSE=1
    - BUILD_TYPE=Release VERBOSE=1 CXX_FLAGS=-std=c++11
