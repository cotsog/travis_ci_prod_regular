language: node_js
  
node_js:
  - "6"
  
services:
  - docker

install: true
script:
 - sudo cat /etc/docker/deamon.json
 - sudo cat /etc/docker/deamon.json | jq 'del(."registry-mirrors")'
 - sudo cat /etc/docker/deamon.json | jq 'del(."registry-mirrors")' | sudo tee /etc/docker/deamon.json
 - sudo cat /etc/docker/deamon.json

notifications:
  email: false
