language: python
matrix:
  include:
    - sudo: false
      dist: trusty
      env: SUDO=false DIST=trusty
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty
addons:
    apt:
      sources:
        - debian-sid
      packages:
        - shellcheck
install: true
script: true
