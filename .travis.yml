matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

install: true
script:
  - sudo add-apt-repository ppa:xapian-backports/ppa
  - sudo apt-get update

notifications:
  email: false
