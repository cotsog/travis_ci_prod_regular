matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial

addons:
  apt:
    packages:
    - openvpn

install: true
script: true

notifications:
  email: false
