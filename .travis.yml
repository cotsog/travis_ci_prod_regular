language: php
php: 7.1

matrix:
  include:
    - sudo: false
    - sudo: required
      group: deprecated-2017Q3
    - sudo: required
      group: stable

install: true
script:
  - composer config --list
  - composer config --list --global
