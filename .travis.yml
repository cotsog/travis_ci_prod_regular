matrix:
    include:
      - language: android
        sudo: false
        dist: trusty
        env: CORDOVA_PLATFORMS=android
        android:
          components: [tools, android-25, build-tools-25.0.2]
        install: true
        script: true

      - language: objective-c
        osx_image: xcode9.2
        install: true
        script: true

install: true
script: true
