dist: bionic

language: python

services:
  - memcached
  - redis-server

addons:
  postgresql: "9.6"
  apt:
    update: true
    sources:
      - google-chrome
    packages:
      - libxmlsec1-dev
      - libgeoip-dev
      - google-chrome-stable

install: true
script: true

notifications:
  email: false
