language: php

php: 7.2

matrix:
  include:
    - dist: xenial
      env: DIST=xenial

install: skip
script: python -c 'import multiprocessing;print multiprocessing.cpu_count()'

notifications:
  email: false
