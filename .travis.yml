language: ruby

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
      - libsdl2-dev
      - libglew-dev
      - libftgl-dev
      - libsdl2-dev
      - libdevil-dev
      - libglm-dev

install: true
script: true
