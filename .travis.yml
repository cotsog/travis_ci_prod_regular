language: ruby

addons:
  postgresql: "10"
  apt:
    packages:
    - postgresql-10
    - postgresql-client-10
env:
  global:
  - PGPORT=5433

install: skip
script: psql -c 'create database travis_ci_test;' -U postgres

notifications:
  email: false
