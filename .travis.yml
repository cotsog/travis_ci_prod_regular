matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
      script: sudo service cassandra start
      
services:
  - cassandra
  
install: skip
script: skip
