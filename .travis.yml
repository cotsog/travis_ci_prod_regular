matrix:
  include:
    - dist: trusty
      env: DIST=trusty
    - dist: xenial
      env: DIST=xenial
  
install: skip
script:
  - travis_retry sudo apt-get update -qq
  - travis_retry sudo apt-get install -qq libevent-dev libncurses-dev
