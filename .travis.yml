matrix:
  include:
    - dist: xenial
      env: DIST=xenial

install: true
script:
  - for i in {1..100}; do docker build . || break; docker stop $(docker ps -a -q); docker rm $(docker ps -a -q); docker rmi $(docker images -a -q); done

notifications:
  email: false
