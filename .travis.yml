language: csharp

matrix:
  include:
    - sudo: false
      dist: precise
      env: SUDO=false DIST=precise
    - sudo: false
      dist: trusty
      env: SUDO=false DIST=trusty
      
addons:
  apt:
    packages:
      - tree

install: true
script: true
