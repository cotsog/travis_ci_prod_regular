language: node_js
node_js:
  - "11"
  - "10"
install: echo "npm ci"
script: echo "npm run test-ci"
after_success: echo "npm run coveralls"
stages:
  - test
  - npm release
jobs:
  include:
    - stage: npm release
      install: skip
      script: true
      after_success: skip
      deploy:
        provider: script
        script: echo "deploy!"
