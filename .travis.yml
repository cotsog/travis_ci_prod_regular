matrix:
  include:
    - sudo: false
    - sudo: required

language: python
python: 3.6

install: true
script:
  - pip install pipenv
  - pipenv install backports-ssl-match-hostname
  - pip install backports-ssl-match-hostname
