language: ruby

matrix:
  include:
    - sudo: false
      dist: trusty
      env: SUDO=false DIST=trusty
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty GROUP=deprecated-2017Q4
      group: deprecated-2017Q4

install: true
script: ip link list
