language: node_js

env:
  global:
    - CACHE_NAME=COVERALL-SHARED-CACHE
    
cache:
  directories:
    - coverall-shared-dir

install: true
script:
  - touch ./coverall-shared-dir/file-to-cache-$TRAVIS_JOB_ID.txt
  - ls -al ./coverall-shared-dir

notifications:
  email: false
