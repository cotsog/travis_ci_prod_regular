matrix:
  include:
    - language: swift
      osx_image: xcode10.1
    - os: linux
notifications:
  email: false
install: skip
script: chmod +x ./deploy.sh
deploy:
  provider: script
  script: deploy.sh
  skip_cleanup: true
  on:
    all_branches: true
