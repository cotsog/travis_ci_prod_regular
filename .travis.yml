rvm:
  - 2.2
  - 2.3

env:
  - FOO=foo
  - FOO=bar

install: skip
script:
  - if [ $TRAVIS_RUBY_VERSION == 2.2 ] && [ $FOO == bar ]; then false; fi
  - echo $FOO

jobs:
  include:
    - stage: deploy
      install: skip
      script: echo Deploy
      # Keep in mind to overwrite these here
