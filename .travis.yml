language: ruby

jobs:
  include:
    - stage: test
      script: echo "./test 1"
    - # stage name not required, will continue to use `test`
      script: echo "./test 2"
    - stage: deploy
      if: env(PRIOR_VERSION) IS present
      script: echo "./deploy"

install: true
script: true
