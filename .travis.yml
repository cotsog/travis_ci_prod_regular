sudo: false
language: node_js
matrix:
  include:
    - env: FIREFOX_VERSION="latest"
    - env: FIREFOX_VERSION="default"
install: true
script:
  - which firefox
  - firefox --version
addons:
  firefox: $FIREFOX_VERSION
